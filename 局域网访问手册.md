# 局域网共享访问操作手册

本工具默认可以**双击 `index.html` 直接在本机使用**。如果需要在同一个 WiFi 下的其他电脑上打开，需要启动一个本地 HTTP 服务，让其他设备通过局域网 IP 访问。

---

## 一、快速开始（推荐）

### 第 1 步：在提供服务的电脑上启动服务

双击项目文件夹中的 **`启动局域网服务.bat`**。

窗口会显示类似如下内容：

```
========================================
  服务已启动！

  本机访问:  http://localhost:8080
  局域网访问: http://192.168.1.100:8080
========================================
```

> 记下"局域网访问"那一行的地址（上面例子中是 `http://192.168.1.100:8080`）。

### 第 2 步：其他电脑访问

1. 确保其他电脑连接到**同一个 WiFi**（同一个局域网）。
2. 打开浏览器（Edge、Chrome 均可）。
3. 输入第 1 步显示的局域网地址，例如 `http://192.168.1.100:8080`。
4. 页面打开后即可正常上传 Excel、提取图片、打包下载。

### 第 3 步：停止服务

使用完毕后，直接关闭那个黑色命令行窗口即可停止服务。

---

## 二、手动启动方式（不用批处理文件）

如果不想用批处理文件，可以手动操作：

### 方法 A：Python（推荐，电脑已装 Python）

1. 打开项目文件夹 `D:\网址\Excel图片提取网`。
2. 在文件夹地址栏输入 `cmd`，按回车，打开命令行窗口。
3. 输入以下命令并回车：

```bash
python -m http.server 8080
```

4. 窗口显示 `Serving HTTP on :: port 8080` 即启动成功。
5. 查看本机 IP：另开一个命令行窗口，输入 `ipconfig`，找到"无线局域网适配器 WLAN"下的"IPv4 地址"。
6. 其他电脑访问 `http://你的IP:8080`。

### 方法 B：Node.js

如果电脑装了 Node.js，可以用：

```bash
npx serve . -l 8080
```

---

## 三、查看本机 IP 地址

如果批处理文件没有正确显示 IP，可以手动查看：

1. 按 `Win + R`，输入 `cmd`，回车。
2. 输入 `ipconfig`，回车。
3. 找到 **"无线局域网适配器 WLAN"**（或"无线局域网适配器 Wi-Fi"）这一段。
4. 记下 **"IPv4 地址"**，通常是 `192.168.x.x` 或 `10.x.x.x` 格式。

> 注意：不要用"以太网适配器"的 IP（那是网线的），也不要用 `127.0.0.1`（那是本机回环地址，其他电脑访问不到）。

---

## 四、常见问题

### Q1：其他电脑打不开页面，显示"无法访问此网站"

**可能原因及解决：**

1. **不在同一个 WiFi** —— 确认两台电脑连的是同一个路由器/热点。
2. **Windows 防火墙拦截** —— 首次启动 Python 服务时，Windows 会弹出防火墙提示，务必勾选"专用网络"并点"允许访问"。如果之前点了取消，见下方"防火墙设置"。
3. **IP 地址变了** —— 电脑重启或重新连 WiFi 后，IP 可能变化，重新用 `ipconfig` 查看。
4. **端口被占用** —— 换一个端口，比如 `python -m http.server 8090`，访问时端口也改成 8090。

### Q2：防火墙怎么放行？

1. 打开 **设置 → 网络和 Internet → 高级网络设置 → Windows 防火墙**。
2. 点 **"允许应用通过防火墙"**。
3. 找到 **Python**（或 `python.exe`），确保"专用"列打勾。
4. 如果找不到 Python，点"更改设置"→"允许其他应用"，浏览到 Python 安装目录添加 `python.exe`。

或者，以管理员身份打开命令行，执行：

```bash
netsh advfirewall firewall add rule name="Python HTTP 8080" dir=in action=allow protocol=TCP localport=8080
```

### Q3：手机能访问吗？

可以。手机连同一个 WiFi，用浏览器输入同样的局域网地址即可。但手机上操作 Excel 文件上传和下载不如电脑方便，建议主要在电脑上使用。

### Q4：服务电脑休眠了怎么办？

提供服务的电脑不能休眠或关机，否则其他电脑会断连。可以在 **设置 → 系统 → 电源和睡眠** 中，把"当接通电源时，电脑在经过以下时间后进入睡眠状态"设为"从不"。

### Q5：上传的 Excel 会传到外网吗？

不会。所有处理都在浏览器本地完成（JSZip + Canvas），文件不会上传到任何服务器，数据只在局域网内传输。

---

## 五、注意事项

- 提供服务的电脑和访问电脑必须在**同一个局域网**（同一个 WiFi 或同一个路由器下）。
- 服务窗口**不要关闭**，关闭即停止服务。
- 建议使用 **8080** 端口；如果被占用，换 8090、8888 等均可，访问地址对应修改。
- 本工具完全离线运行，不需要互联网。
